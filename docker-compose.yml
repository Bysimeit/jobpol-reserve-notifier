services:
  notifier:
    build: .
    container_name: jobpol-reserve-notifier
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - DATA_DIR=/app/data
    volumes:
      - ./.env:/app/.env:ro
      - ./data:/app/data
      - ./debug:/app/debug
